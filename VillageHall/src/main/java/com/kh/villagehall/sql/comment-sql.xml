<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>
	<comment>comment-sql.xml</comment>
	
	<!-- 내 댓글 조회  -->
	<entry key="selectMyComment">
		SELECT COMMENT_CONTENT, TO_CHAR(COMMENT_CREATE_DATE, 'YYYY-MM-DD HH24:MI:SS') AS COMMENT_CREATE_DATE, BOARD_TITLE, BOARD_NO
		FROM "COMMENT" C
		JOIN BOARD USING (BOARD_NO)
		JOIN "USER" U ON U.USER_NO = C.USER_NO
		WHERE U.USER_NO = ?
		ORDER BY COMMENT_CREATE_DATE DESC
	</entry>
	
	
	
	
</properties>